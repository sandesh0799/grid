import { Component } from '@angular/core';
import { Routes } from '@angular/router';
import { GridFormComponent } from './components/grid-form/grid-form.component';

export const routes: Routes = [{
    path: '', component: GridFormComponent
}];
